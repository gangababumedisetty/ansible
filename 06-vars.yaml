- name: task  level varible
  hosts: local
  connection: local
  vars_files:
   - course.yaml
  tasks:
    - name: print varible
      vars:
         COURSE: Ansible
      ansible.builtin.debug:
        msg: "Course is: {{ COURSE }},Trainer is: {{TRAINER}}"
        